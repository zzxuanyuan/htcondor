MACRO ( COMMAND_TARGET _TARGET _COMMAND _INARGS _OUTPUT )

add_custom_command(
    OUTPUT ${_OUTPUT}
	COMMAND ${_COMMAND}
	ARGS ${_INARGS}
)

if (NOT WINDOWS)
	add_custom_target(
		${_TARGET}
		ALL
		DEPENDS ${_OUTPUT} )
endif()

ENDMACRO ( COMMAND_TARGET )