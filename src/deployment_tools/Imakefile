NAME = uniq_pid_command
all_target( $(NAME) )

CFLAGS = $(STD_C_FLAGS)
C_PLUS_FLAGS = $(STD_C_PLUS_FLAGS)
LIB = $(STD_LIBS)

OBJ = uniq_pid_tool_main.o 

c_plus_target($(NAME),$(OBJ),$(LIB))

html:
release:: 
stripped:: 
static:: 
clean::


